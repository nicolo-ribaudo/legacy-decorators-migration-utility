@console
  .log
  .bind(console)
export default class Foo {
  @function(desc){desc.initializer=()=>3}

  baz = 2;

  @enumerable
  method(

  ) {
      return  "strange" +
                "indentation";
  }
}
